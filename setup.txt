- This code shows me some of my pages tagged as "learnings" from notion every month
- It's deployed as a lambda function, and runs off a monthly cloudwatch eventbridge trigger
- Environment variables and Triggers are set manually in Lambda via the UI
